# Copy to `.env.registry` and fill in your Kakao Container Registry credentials.
# `docker_build.sh` sources this file before `docker login` and `docker push`.
# Never commit the real `.env.registry` — it is listed in .gitignore.

REGISTRY_USERNAME=your-kcr-username
REGISTRY_PASSWORD=your-kcr-password
